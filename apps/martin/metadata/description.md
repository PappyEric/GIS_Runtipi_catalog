# Martin Tile Server

Martin is a blazing fast Vector Tile Server for PostGIS.

## Features
- Generates Vector Tiles (MVT) on the fly from PostGIS tables.
- Supports function-based tile sources.
- Built in Rust for performance.

## Usage
Connects to your `gis-postgres` database by default. Ensure the connection string matches your database credentials.
