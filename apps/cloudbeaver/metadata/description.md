# CloudBeaver

Web-based database management tool.

## Usage
1. Open CloudBeaver.
2. Login with standard credentials (admin/admin first time).
3. Connect to your database using the host `gis-postgres` and port `5432`.
